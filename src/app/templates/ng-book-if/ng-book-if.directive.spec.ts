import { NgBookIfDirective } from './ng-book-if.directive';

describe('NgBookIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgBookIfDirective();
    expect(directive).toBeTruthy();
  });
});
